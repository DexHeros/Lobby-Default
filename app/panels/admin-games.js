import { createStub } from './_stub.js';
export default createStub({
    id: 'admin-games', title: 'Admin', breadcrumb: ['ADMIN', 'GAMES'],
    variant: 'right', width: 720, stageMode: 'dim',
    legacyHref: '/pages/admin-games.html',
    autoEmbed: true,
});
