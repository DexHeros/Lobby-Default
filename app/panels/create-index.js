import { createStub } from './_stub.js';
export default createStub({
    id: 'create-index', title: 'Index', breadcrumb: ['CREATE', 'INDEX'],
    variant: 'right', width: 440, stageMode: 'keep',
    legacyHref: '/pages/create-index.html',
    blurb: 'Choose the chain for your index.',
});
