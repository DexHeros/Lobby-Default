import { createStub } from './_stub.js';
export default createStub({
    id: 'register-game', title: 'Register Game', breadcrumb: ['CREATE', 'GAME'],
    variant: 'right', width: 720, stageMode: 'keep',
    legacyHref: '/pages/register-game-form.html',
    autoEmbed: true,
});
