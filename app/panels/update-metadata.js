import { createStub } from './_stub.js';
export default createStub({
    id: 'update-metadata', title: 'Metadata', breadcrumb: ['MANAGE', 'METADATA'],
    variant: 'right', width: 640, stageMode: 'keep',
    legacyHref: '/pages/update-metadata.html',
    autoEmbed: true,
});
