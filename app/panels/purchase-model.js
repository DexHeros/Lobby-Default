import { createStub } from './_stub.js';
export default createStub({
    id: 'purchase-model', title: 'Purchase', breadcrumb: ['MODELS', 'BUY'],
    variant: 'right', width: 640, stageMode: 'context',
    legacyHref: '/pages/purchase-model.html',
    autoEmbed: true,
    parentHash: '#/models',
});
