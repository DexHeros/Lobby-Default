import { createStub } from './_stub.js';
export default createStub({
    id: 'referrals', title: 'Referrals', breadcrumb: ['REFERRALS'],
    variant: 'right', width: 560, stageMode: 'keep',
    legacyHref: '/pages/referrals.html',
    autoEmbed: true,
});
